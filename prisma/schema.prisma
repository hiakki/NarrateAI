generator client {
  provider = "prisma-client-js"
}


datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// ─── Enums ───────────────────────────────────────────────

enum UserRole {
  OWNER
  ADMIN
  USER
}

enum UserPlan {
  FREE
  STARTER
  PRO
  AGENCY
}

enum SeriesStatus {
  ACTIVE
  PAUSED
}

enum VideoStatus {
  QUEUED
  GENERATING
  REVIEW
  READY
  SCHEDULED
  POSTED
  FAILED
}

enum GenerationStage {
  SCRIPT
  TTS
  IMAGES
  I2V
  ASSEMBLY
  UPLOADING
}

enum Platform {
  INSTAGRAM
  YOUTUBE
  FACEBOOK
  SHARECHAT
  MOJ
}

enum SubscriptionStatus {
  ACTIVE
  PAST_DUE
  CANCELED
}

enum LlmProvider {
  GEMINI_FLASH
  OPENAI_GPT4O_MINI
  DEEPSEEK_V3
  QWEN
  LOCAL_LLM
  LOCAL_BACKEND
  HF_STORY
  HF_STORY_ZEPHYR
  HF_STORY_MISTRAL_INSTRUCT
  HF_STORY_PHI2
  HF_STORY_LLAMA_TINY
  HF_STORY_SMOLLAMA
  HF_STORY_QWEN_7B
}

enum TtsProvider {
  GEMINI_TTS
  ELEVENLABS
  COSYVOICE
  FISH_AUDIO
  EDGE_TTS
  HF_TTS
  LOCAL_BACKEND
}

enum ImageProvider {
  GEMINI_IMAGEN
  DALLE3
  FLUX
  KOLORS
  SDXL
  FLUX_SCHNELL
  TOGETHER
  SILICONFLOW
  LEONARDO
  IDEOGRAM
  POLLINATIONS
  HF_IMAGE
  LOCAL_BACKEND
}

// ─── Models ──────────────────────────────────────────────

model User {
  id            String    @id @default(cuid())
  email         String    @unique
  name          String?
  passwordHash  String?
  avatarUrl     String?
  emailVerified Boolean   @default(false)
  role          UserRole  @default(USER)
  plan          UserPlan  @default(FREE)
  stripeCustomerId String?

  defaultLlmProvider   LlmProvider?
  defaultTtsProvider   TtsProvider?
  defaultImageProvider ImageProvider?
  defaultImageToVideoProvider String?
  lastCreatePrefs      Json?

  createdAt     DateTime  @default(now())
  updatedAt     DateTime  @updatedAt

  series         Series[]
  automations    Automation[]
  characters     Character[]
  socialAccounts SocialAccount[]
  subscription   Subscription?

  @@map("users")
}

model EmailVerification {
  id        String   @id @default(cuid())
  email     String
  code      String
  expiresAt DateTime
  attempts  Int      @default(0)
  used      Boolean  @default(false)
  createdAt DateTime @default(now())

  @@index([email, code])
  @@index([email, expiresAt])
  @@map("email_verifications")
}

model Series {
  id              String       @id @default(cuid())
  userId          String
  name            String
  niche           String
  artStyle        String       @default("realistic")
  musicType       String?
  musicUrl        String?
  voiceId         String?
  language        String       @default("en")
  tone            String       @default("dramatic")
  status          SeriesStatus @default(ACTIVE)

  characterId     String?

  llmProvider      LlmProvider?
  ttsProvider      TtsProvider?
  imageProvider    ImageProvider?

  createdAt       DateTime     @default(now())
  updatedAt       DateTime     @updatedAt

  user            User         @relation(fields: [userId], references: [id], onDelete: Cascade)
  character       Character?   @relation(fields: [characterId], references: [id], onDelete: SetNull)
  videos          Video[]
  automation      Automation?

  @@index([userId])
  @@map("series")
}

model Automation {
  id              String        @id @default(cuid())
  userId          String
  name            String
  niche           String
  artStyle        String        @default("realistic")
  voiceId         String?
  language        String        @default("en")
  tone            String        @default("dramatic")
  duration        Int           @default(45)

  characterId     String?

  llmProvider     LlmProvider?
  ttsProvider     TtsProvider?
  imageProvider   ImageProvider?
  imageToVideoProvider String?

  targetPlatforms Json          @default("[]")
  enabled         Boolean       @default(true)
  includeAiTags   Boolean       @default(true)
  frequency       String        @default("daily")
  postTime        String        @default("09:00")
  timezone        String

  lastRunAt       DateTime?
  seriesId        String?       @unique

  createdAt       DateTime      @default(now())
  updatedAt       DateTime      @updatedAt

  user            User          @relation(fields: [userId], references: [id], onDelete: Cascade)
  character       Character?    @relation(fields: [characterId], references: [id], onDelete: SetNull)
  series          Series?       @relation(fields: [seriesId], references: [id], onDelete: SetNull)

  @@index([userId])
  @@index([enabled])
  @@map("automations")
}

model Video {
  id               String           @id @default(cuid())
  seriesId         String
  title            String?
  description      String?
  scriptText       String?
  scenesJson       Json?
  voiceoverUrl     String?
  videoUrl         String?
  thumbnailUrl     String?
  duration         Int?
  targetDuration   Int?
  status           VideoStatus      @default(QUEUED)
  generationStage  GenerationStage?
  scheduledPostTime DateTime?
  postedPlatforms  Json             @default("[]")
  errorMessage     String?
  checkpointData   Json?
  stageTimings     Json?   // per-stage durations: { SCRIPT: { startedAt, completedAt, durationMs }, ... }
  retryCount       Int              @default(0)
  insights         Json?
  insightsRefreshedAt DateTime?

  createdAt        DateTime         @default(now())
  updatedAt        DateTime         @updatedAt

  series           Series           @relation(fields: [seriesId], references: [id], onDelete: Cascade)

  @@index([seriesId])
  @@index([status])
  @@map("videos")
}

model Character {
  id          String   @id @default(cuid())
  userId      String
  name        String
  type        String   @default("human")
  physical    String?
  clothing    String?
  accessories String?
  features    String?
  personality String?
  fullPrompt  String
  previewUrl  String?

  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  user        User         @relation(fields: [userId], references: [id], onDelete: Cascade)
  series      Series[]
  automations Automation[]

  @@index([userId])
  @@map("characters")
}

model SocialAccount {
  id              String   @id @default(cuid())
  userId          String
  platform        Platform
  platformUserId  String
  pageId          String?
  pageName        String?
  accessTokenEnc  String
  refreshTokenEnc String?
  username        String?
  profileUrl      String?
  connectedAt     DateTime @default(now())
  tokenExpiresAt  DateTime?
  metrics         Json?
  metricsRefreshedAt DateTime?
  metricsBaseline Json?

  user            User     @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([userId, platform, platformUserId])
  @@index([userId])
  @@map("social_accounts")
}

model AdminSettings {
  id                    String   @id @default("singleton")
  enabledLlmProviders   Json     @default("[\"GEMINI_FLASH\",\"OPENAI_GPT4O_MINI\",\"DEEPSEEK_V3\",\"QWEN\"]")
  enabledTtsProviders   Json     @default("[\"GEMINI_TTS\",\"ELEVENLABS\",\"COSYVOICE\",\"FISH_AUDIO\",\"EDGE_TTS\",\"HF_TTS\"]")
  enabledImageProviders Json     @default("[\"GEMINI_IMAGEN\",\"DALLE3\",\"FLUX\",\"KOLORS\",\"SDXL\",\"FLUX_SCHNELL\",\"TOGETHER\",\"SILICONFLOW\",\"LEONARDO\",\"IDEOGRAM\",\"POLLINATIONS\",\"HF_IMAGE\",\"LOCAL_BACKEND\"]")
  updatedAt             DateTime @updatedAt

  @@map("admin_settings")
}

model Subscription {
  id                   String             @id @default(cuid())
  userId               String             @unique
  stripeSubscriptionId String             @unique
  stripePriceId        String?
  status               SubscriptionStatus @default(ACTIVE)
  currentPeriodStart   DateTime?
  currentPeriodEnd     DateTime?

  createdAt            DateTime           @default(now())
  updatedAt            DateTime           @updatedAt

  user                 User               @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@map("subscriptions")
}
